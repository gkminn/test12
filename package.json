{
  "name": "autostock-ai",
  "private": true,
  "version": "0.1.0",
  "description": "AutoStock AI - search-driven Claude stock prediction",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w @autostock/api\" \"npm run dev -w @autostock/web\"",
    "build": "npm run build -w @autostock/shared && npm run build -w @autostock/api && npm run build -w @autostock/web",
    "test": "npm run test -w @autostock/api"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
